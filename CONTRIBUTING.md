# Contributing

شكرًا لرغبتك بالمساهمة! خطوات سريعة:

1. افتح Issue لوصف التغيير أو المشكلة قبل البدء.
2. أنشئ فرعًا جديدًا من الفرع `main` أو `master` باسم وصفي (مثال: `feat/auth` أو `fix/login-bug`).
3. التزم بقواعد الترميز التالية:
   - استخدم تنسيق الملفات/الـ lint المناسب للّغة (Prettier/ESLint للـ JS، black/flake8 للـ Python).
   - اكتب اختبارات صغيرة لكل تغيير وظيفي إن أمكن.
4. افتح Pull Request وأضف وصفًا للتغييرات وكيف يمكن اختبارها.
5. انتظر المراجعة وسيتم دمج التغييرات بعد الموافقة والاختبارات.

إذا احتجت مساعدة في إعداد بيئة التطوير المحلية أو CI لّغة معينة، اذكر اللغة وسأجهز الملفات المناسبة.
